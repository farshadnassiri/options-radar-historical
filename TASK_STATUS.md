# وضعیت تسک‌های پروژه

آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۱۴  
نسخه برنامه: `0.5.0`  
آخرین نسخه ثبت‌شده در گیت هنگام تهیه این گزارش: `ddf02e2`

این فایل مرجع خلاصه وضعیت خواسته‌های کاربر است. جزئیات فنی هر تغییر در
`WORKLOG.md` نگهداری می‌شود. بعد از هر تغییر مهم، وضعیت این فایل نیز باید
به‌روزرسانی شود.

## خلاصه پیشرفت

| بخش | انجام‌شده | نیمه‌کاره | باقی‌مانده | متوقف/لغوشده به درخواست کاربر |
|---|---:|---:|---:|---:|
| قابلیت‌های مستقیم برنامه | ۱۹ از ۱۹ (۱۰۰٪) | ۰ | ۰ | ۰ |
| اجرای محلی و مخزن GitHub | ۲ از ۲ (۱۰۰٪) | ۰ | ۰ | ۰ |
| توسعه خودکار ساعتی محلی | ۰ | ۰ | ۰ | ۱ (متوقف موقت) |
| اجرای خودکار هنگام خاموش‌بودن لپ‌تاپ | ۰ | ۰ | ۰ | ۱ |
| بهبودهای فنی اضافه، خارج از خواسته‌های اصلی | ۵۱ انجام‌شده | ۰ | ۵ | ۰ |

در نتیجه، از **۲۱ خواسته فعال و مستقیم، هر ۲۱ مورد پیاده‌سازی شده‌اند**.
توسعه ساعتی در ۲۰۲۶/۰۸/۱۴ با درخواست کاربر موقتاً متوقف شد. یک خواسته
جداگانه درباره اجرای ابری هنگام خاموش‌بودن لپ‌تاپ نیز با پیام «فعلاً
بی‌خیال» لغو شد. هیچ‌کدام کار فعال باقی‌مانده محسوب نمی‌شوند.

> «انجام‌شده» یعنی کد، رابط و آزمون آن در پروژه موجود است؛ به معنی تضمین
> سودآوری مالی یا تأیید همه اعداد با داده زنده همه کارگزاری‌ها نیست.

## خواسته‌های مستقیم برنامه

| ردیف | خواسته | وضعیت | محل اصلی پیاده‌سازی یا شاهد |
|---:|---|---|---|
| ۱ | بازپخش استراتژی از تاریخ شروع تا پایان، یک ردیف برای هر روز معاملاتی همراه تاریخ و نام روز | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۲ | انتخاب مستقل مبنای قیمت ورود و آفست از آخرین، پایانی، کمترین و بیشترین | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۳ | ورود دستی قیمت برای هر پای استراتژی، حتی بیرون بازه قیمت همان روز | انجام‌شده | `core/history.mjs`، آزمون‌های بخش ۳۲ |
| ۴ | حفظ وضعیت «فاقد داده» بدون جایگزینی قیمت روز قبل یا قیمت مدل | انجام‌شده | `core/history.mjs` |
| ۵ | توقف بررسی در اولین سررسید، چون هدف نوسان‌گیری است | انجام‌شده | `core/history.mjs`، آزمون توقف سررسید |
| ۶ | بررسی قراردادهای فعال از اولین روز معامله موجود تا آخرین روز | انجام‌شده | `server/server.mjs`، `worker/history-worker.mjs` |
| ۷ | انتخاب نماد پایه، تاریخ شروع و تاریخ پایان با کنترل‌های ریلی | انجام‌شده | `ui/tabs/history.mjs`، `ui/style.css` |
| ۸ | دو حالت انتخاب دستی قراردادها و ساخت همه ترکیب‌های ممکن فقط برای استراتژی انتخابی | انجام‌شده | `worker/history-worker.mjs`، `strategies/catalog.mjs` |
| ۹ | شناسایی سررسیدها و انتخاب یک، چند یا همه سررسیدها | انجام‌شده | `ui/tabs/history.mjs`، نسخه `0.5.0` |
| ۱۰ | نمایش نماد پایه و درصد تغییر/بازده آن در ردیف‌های جدول اصلی | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۱ | حذف فیلتر زیر سرستون و مرتب‌سازی صعودی/نزولی با کلیک یا صفحه‌کلید | انجام‌شده | `ui/table.mjs` |
| ۱۲ | دریافت حجم و ارزش پایه و قراردادها و فیلتر نقدشوندگی زمان اجرا | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۳ | تعداد و درصد معاملات سودده، زیان‌ده و سربه‌سر و توزیع نتایج | انجام‌شده | `core/history.mjs`، کارت‌ها و نمودار توزیع |
| ۱۴ | آمار کامل هر استراتژی به‌صورت روزانه، کلی و بر اساس روز هفته | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۵ | نمایش وجه تضمین کل و تفکیک روزانه هر پای فروش در صورت وجود | انجام‌شده | `core/margin.mjs`، `core/history.mjs` |
| ۱۶ | نمایش اثر تجمعی و تغییر روزانه سود و زیان هر پا در هر مرحله | انجام‌شده | `core/history.mjs`، جدول و CSV تاریخی |
| ۱۷ | تعیین بهترین خروج مشاهده‌شده، قیمت آفست هر پا و قاعده هدف سود/حداکثر روز | انجام‌شده | بهینه‌ساز `core/history.mjs` |
| ۱۸ | نمودارهای زمانی، توزیع، پراکندگی، حساسیت قیمت و ماتریس ۱۶ مبنای ورود/خروج | انجام‌شده | `ui/tabs/history.mjs`، `ui/chart.mjs` |
| ۱۹ | نمایش سرمایه درگیر، مبلغ پرداختی، مبلغ دریافتی و خالص جریان نقدی | انجام‌شده | `core/history.mjs`، نسخه `0.5.0` |

## اجرا، ذخیره و انتشار کد

| خواسته | وضعیت | توضیح |
|---|---|---|
| اجرای برنامه به‌صورت محلی در صفحه وب | انجام‌شده | اجرا با `node server/server.mjs` و نشانی `http://127.0.0.1:8787` |
| ذخیره محلی، ثبت در Git و Push به یک مخزن جدید | انجام‌شده | فایل‌ها ابتدا در همین پوشه ذخیره می‌شوند و سپس به شاخه `main` مخزن خصوصی GitHub پوش می‌شوند |
| اجرای خودکار توسعه هر یک ساعت و Push بعد از تغییر کامل | متوقف موقت به درخواست کاربر | تنظیمات آن حفظ شده ولی وضعیت تسک `PAUSED` است و تا فعال‌سازی دوباره اجرا نمی‌شود |
| ادامه کار هنگام خاموش‌بودن لپ‌تاپ | لغوشده به درخواست کاربر | راه ابری/GitHub Actions پیاده نشد؛ کاربر گفت «فعلاً بی‌خیال» |

مخزن مقصد:
<https://github.com/farshadnassiri/options-radar-historical>

طبق [مستندات رسمی OpenAI درباره Scheduled tasks](https://learn.chatgpt.com/docs/automations)،
تسکی که به فایل‌های پروژه محلی نیاز دارد فقط با روشن‌بودن رایانه و در حال
اجرا بودن برنامه دسکتاپ می‌تواند به همان پوشه دسترسی داشته باشد.

## موارد فنی باقی‌مانده خارج از خواسته‌های اصلی

این موارد مانع استفاده از قابلیت تحلیل تاریخی نیستند، ولی برای پاک‌سازی
فنی آینده در `WORKLOG.md` باز مانده‌اند:

1. اتصال صحیح چند `label` به کنترل متناظر در سه تب برای دسترس‌پذیری.
2. جایگزینی `aria-pressed` با `aria-expanded` در دکمه پنل ستون‌ها.
3. نمایش خنثی مقدار نامعتبر سناریوی شوک به‌جای رنگ زیان در یک حالت مرزی.
4. محلی‌کردن قلم رابط برای حذف وابستگی اختیاری قلم به CDN.
5. تطبیق اختلاف ۴۰٬۰۰۰ ریالی یک نمونه وجه تضمین (`ضهرم5033`)؛ نیازمند
   داده خام همان لحظه از تابلو است.

## معیار کنترل فعلی

- آزمون‌های ثبت‌شده: **۴۲۸ قبول، صفر رد**.
- شاخه انتشار: `main`.
- فایل جزئیات تغییرات: `WORKLOG.md`.
- راهنمای توسعه ساعتی: `AUTOMATION.md`.
